{{#each posts as |post|}}
  {{#if post.isBlogPost}}
    <div class="blogpost-title"><h2>{{post.title}}</h2></div>
    <div class="blogpost-description">{{{post.description}}}</div>
  {{else}}
    <div class="post-title"><h2>{{post.title}}</h2></div>
    <div class="post-title"><h3>{{post.subtitle}}</h3></div>
    <div class="post-description">{{{post.description}}}</div>
    <div class="tags">{{#each post.tags as |tag|}}<small>{{tag}}</small>{{/each}}</div>
  {{/if}}
  <hr>
{{/each}}
{{yield}}
